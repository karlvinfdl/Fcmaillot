{
  "name": "fcmaillot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve . -p 3000 --no-clipboard",
    "build": "node build.js",
    "deploy": "npm run build && npx gh-pages -d dist --dotfiles"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "gh-pages": "^6.1.1",
    "terser": "^5.27.0"
  }
}
